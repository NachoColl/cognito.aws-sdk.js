language: node_js
node_js:
  - "7"
install:
  - npm install
script: 
  - gulp compress
before_deploy:
  - pip install awscli 
deploy:
  provider: scrit
  script:
    - aws s3 sync ./dist s3://$AWS_PATHTESTS/$TRAVIS_BUILD_NUMBER


